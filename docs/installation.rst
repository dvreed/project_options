============
Installation
============

At the command line::

    $ easy_install project_options

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv project_options
    $ pip install project_options
